using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Game : MonoBehaviour
{

    public static bool alive = true;
    public static GameObject[] buzzsaws = {};

    // Start is called before the first frame update
    void Start()
    {
        
    }
}
